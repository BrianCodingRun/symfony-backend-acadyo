doctrine_mongodb:
    connections:
        default:
            server: "%env(resolve:MONGODB_URL)%"
    default_database: "%env(MONGODB_DB_TEST)%"
    document_managers:
        default:
            auto_mapping: true
